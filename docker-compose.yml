services:
  db:
    image: postgis/postgis:16-3.4
    restart: unless-stopped
    environment:
      POSTGRES_USER: ronda
      POSTGRES_PASSWORD: ronda_dev
      POSTGRES_DB: ronda
    ports:
      - "5432:5432"
    volumes:
      - ronda_pgdata:/var/lib/postgresql/data

volumes:
  ronda_pgdata:
